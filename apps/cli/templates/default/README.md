# JiraTown App

```bash
pnpm install
jiratown dev
```

Client: http://localhost:3000

Server: http://localhost:4000

Use `jiratown connect doctor` to check local configuration and `jiratown connect jira`, `jiratown connect obsidian`, or `jiratown connect reminders` to write `.env.local`.
